# ===================================================================
#                    TESTE DE OFFSET PIEZO vs BLTOUCH
# ===================================================================
# Este arquivo contém macros para testar e calibrar o offset entre
# o sensor piezoelétrico e o BLTouch
#
# POSIÇÕES:
# - BLTouch: offset X40 Y-50 (em relação ao bico)
# - Sensor Piezo: X320 Y100 (posição absoluta na mesa)
# ===================================================================

[gcode_macro TST_OFS_PZB]
description: Testa offset entre sensor piezoelétrico e BLTouch
gcode:
    RESPOND MSG="🎯 ===== TESTE OFFSET PIEZO vs BLTOUCH ====="
    RESPOND MSG="📍 BLTouch: offset X40 Y-50 (relativo ao bico)"
    RESPOND MSG="📍 Sensor Piezo: X320 Y100 (posição absoluta)"
    
    # Salvar posição atual
    SAVE_GCODE_STATE NAME=teste_offset
    
    # Garantir que estamos em modo absoluto
    G90
    
    # 1. Fazer home para estabelecer referência
    RESPOND MSG="🏠 Fazendo home para estabelecer referência..."
    G28
    
    # 2. Obter posição atual do bico após G28
    {% set home_x = printer.toolhead.position.x %}
    {% set home_y = printer.toolhead.position.y %}
    {% set home_z = printer.toolhead.position.z %}
    
    RESPOND MSG="📌 Posição após G28: X{home_x} Y{home_y} Z{home_z}"
    
    # 3. Calcular posição do BLTouch em coordenadas absolutas
    {% set bltouch_abs_x = home_x + 40 %}
    {% set bltouch_abs_y = home_y - 50 %}
    
    RESPOND MSG="📍 BLTouch em coordenadas absolutas: X{bltouch_abs_x} Y{bltouch_abs_y}"
    
    # 4. Verificar se a posição está dentro dos limites (X_max=325, Y_max=280)
    {% if bltouch_abs_x > 325 %}
        {% set bltouch_safe_x = 325 %}
        RESPOND MSG="⚠️ AVISO: X{bltouch_abs_x} excede limite! Usando X{bltouch_safe_x}"
    {% else %}
        {% set bltouch_safe_x = bltouch_abs_x %}
    {% endif %}
    
    {% if bltouch_abs_y < 0 %}
        {% set bltouch_safe_y = 0 %}
        RESPOND MSG="⚠️ AVISO: Y{bltouch_abs_y} abaixo do limite! Usando Y{bltouch_safe_y}"
    {% else %}
        {% set bltouch_safe_y = bltouch_abs_y %}
    {% endif %}
    
    # 5. Calcular diferença para o sensor piezo
    {% set offset_x = 320 - bltouch_safe_x %}
    {% set offset_y = 100 - bltouch_safe_y %}
    
    RESPOND MSG="🔧 OFFSET CALCULADO:"
    RESPOND MSG="   Piezo X320 - BLTouch X{bltouch_safe_x} = {offset_x}mm"
    RESPOND MSG="   Piezo Y100 - BLTouch Y{bltouch_safe_y} = {offset_y}mm"
    
    # 6. Testar movimento para ambas as posições (usando coordenadas seguras)
    RESPOND MSG="🚀 Testando movimento para posição do BLTouch..."
    G1 X{bltouch_safe_x} Y{bltouch_safe_y} Z10 F3000
    G4 P2000  # Pausa 2 segundos
    
    RESPOND MSG="🚀 Testando movimento para posição do sensor piezo..."
    G1 X320 Y100 Z10 F3000
    G4 P2000  # Pausa 2 segundos
    
    # 7. Testar aproximação do sensor TP223 com detecção manual
    RESPOND MSG="🔍 Testando aproximação do sensor TP223..."
    RESPOND MSG="⚠️ Resetando estado do sensor TP223..."
    
    # Resetar variáveis do sensor TP223
    SET_GCODE_VARIABLE MACRO=TESTE_TP223_IND VARIABLE=sensor_triggered VALUE=0
    SET_GCODE_VARIABLE MACRO=TESTE_TP223_IND VARIABLE=trigger_z VALUE=0
    
    # Aproximação gradual até ativar o sensor TP223
    RESPOND MSG="📉 Descendo gradualmente até ativar sensor TP223..."
    
    # Descer gradualmente até o sensor ativar
    _DESCIDA_GRADUAL_TP223
    
    # Subir para segurança
    G1 Z10 F1000
    RESPOND MSG="🛡️ Retornando para posição segura Z10"
    
    # 8. Restaurar posição
    RESTORE_GCODE_STATE NAME=teste_offset
    
    RESPOND MSG="✅ Teste concluído!"
    RESPOND MSG="📋 RESUMO DOS OFFSETS:"
    RESPOND MSG="   X offset: {offset_x}mm"
    RESPOND MSG="   Y offset: {offset_y}mm"

# ===================================================================
#                        MACROS AUXILIARES
# ===================================================================

[gcode_macro _DESCIDA_GRADUAL_TP223]
description: Descida gradual até ativar sensor TP223 com parada imediata
gcode:
    # Descer de 1mm em 1mm até o sensor ativar ou atingir Z0
    {% set sensor_ativado = false %}
    {% for step in range(1, 11) %}
        {% if not sensor_ativado %}
            {% set target_z = 10 - step %}
            G1 Z{target_z} F300
            G4 P200  # Pausa para estabilizar
            
            # Verificar se sensor foi ativado após movimento
            {% if  printer["gcode_macro TESTE_TP223_IND"].sensor_triggered == 1 %} #printer['gcode_button sensor_tp223_teste'].state == 'PRESSED' or
                {% set altura_detectada = printer.gcode_move.gcode_position.z %}
                RESPOND MSG="✅ Sensor TP223 ativado! Altura detectada: Z = {altura_detectada}mm"
                {action_respond_info("Sensor ativado - parando descida")}
                # Atualizar variáveis da macro
                SET_GCODE_VARIABLE MACRO=TESTE_TP223_IND VARIABLE=sensor_triggered VALUE=1
                SET_GCODE_VARIABLE MACRO=TESTE_TP223_IND VARIABLE=trigger_z VALUE={altura_detectada}
                # Marcar sensor como ativado para parar o loop
                {% set sensor_ativado = true %}
            {% elif step == 10 %}
                RESPOND MSG="⚠️ AVISO: Sensor TP223 não ativou até Z0. Verifique conexão!"
            {% endif %}
        {% endif %}
    {% endfor %}

[gcode_macro MED_ALT_AMB]
description: Mede altura com BLTouch e sensor piezo para comparação
gcode:
    RESPOND MSG="📏 ===== MEDIÇÃO COMPARATIVA DE ALTURA ====="
    
    # Salvar estado
    SAVE_GCODE_STATE NAME=medicao_altura
    G90
    
    # 1. Fazer home
    G28
    
    # 2. Medir com BLTouch (posição atual após G28)
    RESPOND MSG="🔍 Medindo altura com BLTouch..."
    PROBE
    {% set bltouch_z = printer.probe.last_z_result %}
    RESPOND MSG="📌 BLTouch mediu: Z = {bltouch_z}mm"
    
    # 3. Mover para posição do sensor piezo
    RESPOND MSG="🚀 Movendo para posição do sensor piezo..."
    G1 X320 Y100 Z10 F3000
    
    # 4. Testar sensor piezo (se configurado como probe)
    RESPOND MSG="🔍 Testando sensor piezo na posição X320 Y100..."
    RESPOND MSG="⚠️ Certifique-se que o sensor piezo está ativo!"
    
    # Verificar se sensor está respondendo
    QUERY_PROBE
    
    # 5. Restaurar posição
    RESTORE_GCODE_STATE NAME=medicao_altura
    
    RESPOND MSG="✅ Medição concluída!"
    RESPOND MSG="📋 Resultado BLTouch: Z = {bltouch_z}mm"
    RESPOND MSG="💡 Para medir com piezo, use: PROBE (na posição X320 Y100)"
    RESPOND MSG="📏 Sensor piezo está ~0.9mm abaixo da mesa (z_offset: -0.9)"

[gcode_macro CAL_OFS_PZO]
description: Procedimento para calibrar offset do sensor piezo
gcode:
    RESPOND MSG="⚙️ ===== CALIBRAÇÃO OFFSET SENSOR PIEZO ====="
    RESPOND MSG="📋 PROCEDIMENTO:"
    RESPOND MSG="1. Execute: TESTE_OFFSET_PIEZO_BLTOUCH"
    RESPOND MSG="2. Anote os valores de offset X e Y"
    RESPOND MSG="3. Configure no printer.cfg:"
    RESPOND MSG="   [probe]"
    RESPOND MSG="   pin: !host:gpio22"
    RESPOND MSG="   x_offset: [valor_calculado]"
    RESPOND MSG="   y_offset: [valor_calculado]"
    RESPOND MSG="   z_offset: -0.9  # Sensor 0.9mm abaixo da mesa"
    RESPOND MSG="4. Execute: RESTART"
    RESPOND MSG="5. Execute: PROBE_CALIBRATE"
    RESPOND MSG=""
    RESPOND MSG="⚠️ IMPORTANTE:"
    RESPOND MSG="- Desative BLTouch se usar sensor piezo como probe principal"
    RESPOND MSG="- Ou mantenha ambos para diferentes funções"

# ===================================================================
#                           COMANDOS ÚTEIS
# ===================================================================
# TESTE_OFFSET_PIEZO_BLTOUCH  - Calcula offset entre sensores
# MEDIR_ALTURA_AMBOS_SENSORES - Compara medições
# CALIBRAR_OFFSET_PIEZO       - Guia de calibração
# G1 X320 Y100 Z10 F3000      - Mover para sensor piezo
# QUERY_PROBE                 - Verificar status do probe ativo
# ===================================================================