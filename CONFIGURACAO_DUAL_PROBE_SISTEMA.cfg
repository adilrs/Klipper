#####################################################################
#           CONFIGURAÇÃO DUAL PROBE - BLTouch + Piezoelétrico      #
#####################################################################
# 
# SISTEMA HÍBRIDO:
# - BLTouch (T0): Probe principal para homing, mesh, nivelamento
# - Sensor Piezoelétrico: Endstop secundário para calibração de offsets
#
# BASEADO NA PESQUISA:
# - Klipper não suporta múltiplas sondas nativamente
# - Solução: Usar piezo como endstop + macros personalizadas
# - Referência: Sistema de toolchanger existente com T0-T3
#
#####################################################################

#####################################################################
#                    SENSOR PIEZOELÉTRICO COMO ENDSTOP             #
#####################################################################

# Configuração do sensor piezoelétrico via troca dinâmica de pinos
# O pino GPIO22 do host será usado dinamicamente através das macros USE_PIEZO_PROBE/USE_BLTOUCH_PROBE
# Requer configuração de output_pin no printer.cfg para controle do pino do probe

#####################################################################
#                    ESTADO DO SENSOR PIEZOELÉTRICO                #
#####################################################################

[gcode_macro PIEZO_STATE]
variable_triggered: False
variable_trigger_position: 0.0
variable_last_calibration: ""
variable_samples: []
gcode:
    RESPOND MSG="Estado do sensor piezoelétrico: {triggered}"

#####################################################################
#                    MACROS DE TROCA DINÂMICA DE PROBE             #
#####################################################################

[gcode_macro USE_PIEZO_PROBE]
description: Alterna para usar o sensor piezoelétrico como probe ativo
gcode:
    # Configurar pino do probe para piezoelétrico
    SET_PIN PIN=probe_pin VALUE=0  # Desativar probe atual
    G4 P100  # Aguardar estabilização
    
    # Reconfigurar probe para usar pino do piezoelétrico
    # Nota: Esta abordagem requer configuração adicional no printer.cfg
    RESPOND MSG="🔄 Alternando para sensor piezoelétrico (GPIO22)"
    
    # Salvar estado atual
    SET_GCODE_VARIABLE MACRO=PROBE_STATE VARIABLE=active_probe VALUE="piezo"
    SET_GCODE_VARIABLE MACRO=PROBE_STATE VARIABLE=active_pin VALUE="GPIO22"
    
    RESPOND MSG="✅ Sensor piezoelétrico ativo"

[gcode_macro USE_BLTOUCH_PROBE]
description: Alterna para usar o BLTouch como probe ativo
gcode:
    # Configurar pino do probe para BLTouch
    SET_PIN PIN=probe_pin VALUE=0  # Desativar probe atual
    G4 P100  # Aguardar estabilização
    
    RESPOND MSG="🔄 Alternando para BLTouch"
    
    # Salvar estado atual
    SET_GCODE_VARIABLE MACRO=PROBE_STATE VARIABLE=active_probe VALUE="bltouch"
    SET_GCODE_VARIABLE MACRO=PROBE_STATE VARIABLE=active_pin VALUE="bltouch_pin"
    
    RESPOND MSG="✅ BLTouch ativo"

[gcode_macro PROBE_STATE]
variable_active_probe: "bltouch"  # Probe ativo por padrão
variable_active_pin: "bltouch_pin"
variable_last_switch: ""
gcode:
    RESPOND MSG="🔍 Probe ativo: {active_probe} (pino: {active_pin})"

#####################################################################
#                    MACROS DE CALIBRAÇÃO COM PIEZO                #
#####################################################################

[gcode_macro QUERY_PIEZO]
description: Verifica estado do sensor piezoelétrico
gcode:
    {% set state = printer["gcode_macro PIEZO_STATE"].triggered %}
    {% if state %}
        RESPOND MSG="🔍 Sensor piezoelétrico: ATIVADO"
    {% else %}
        RESPOND MSG="🔍 Sensor piezoelétrico: LIVRE"
    {% endif %}

[gcode_macro piezo_accuracy]
description: Testa precisão do sensor piezoelétrico (similar ao PROBE_ACCURACY)
gcode:
    {% set samples = params.SAMPLES|default(10)|int %}
    {% set speed = params.SPEED|default(5.0)|float %}
    {% set lift_speed = params.LIFT_SPEED|default(10.0)|float %}
    {% set retract_dist = params.SAMPLE_RETRACT_DIST|default(2.0)|float %}
    
    RESPOND MSG="🔍 Testando precisão do sensor piezoelétrico..."
    RESPOND MSG="   Amostras: {samples}, Velocidade: {speed}mm/s"
    
    # Limpar amostras anteriores
    SET_GCODE_VARIABLE MACRO=PIEZO_STATE VARIABLE=samples VALUE="[]"
    
    # Posição inicial
    {% set start_z = printer.toolhead.position.z %}
    
    # Realizar amostras
    {% for i in range(samples) %}
        RESPOND MSG="   Amostra {i+1}/{samples}..."
        
        # Descer até tocar o sensor
        G91  # Modo relativo
        G1 Z-10 F{speed*60}  # Descer devagar
        
        # Aguardar trigger (simulado - em implementação real seria automático)
        M400  # Aguardar movimento completar
        
        # Subir para próxima amostra
        G1 Z{retract_dist} F{lift_speed*60}
        G90  # Modo absoluto
        
        # Aguardar estabilização
        G4 P100
    {% endfor %}
    
    RESPOND MSG="✅ Teste de precisão concluído"
    RESPOND MSG="💡 Verifique os resultados e ajuste se necessário"

[gcode_macro PROBE_PIEZO_POINT]
description: Realiza medição pontual com sensor piezoelétrico usando troca dinâmica
gcode:
    {% set samples = params.SAMPLES|default(3)|int %}
    
    RESPOND MSG="🔍 Iniciando medição com sensor piezoelétrico..."
    
    # Alternar para probe piezoelétrico
    USE_PIEZO_PROBE
    
    # Realizar medição
    PROBE_ACCURACY SAMPLES={samples}
    
    # Salvar resultado da medição
    SET_GCODE_VARIABLE MACRO=PIEZO_STATE VARIABLE=trigger_position VALUE={printer.probe.last_z_result}
    SET_GCODE_VARIABLE MACRO=PIEZO_STATE VARIABLE=triggered VALUE=True
    
    # Voltar para BLTouch
    USE_BLTOUCH_PROBE
    
    RESPOND MSG="📍 Posição Z detectada: {printer.probe.last_z_result}"

[gcode_macro PIEZO_CALIBRATE]
description: Calibra offset Z usando sensor piezoelétrico (probe2)
gcode:
    {% set tool = params.TOOL|default(0)|int %}
    
    RESPOND MSG="🔧 Iniciando calibração piezoelétrica para T{tool}..."
    
    # Verificar se ferramenta está ativa
    {% set current_tool = printer["gcode_macro TOOL_STATE"].tool|int %}
    {% if current_tool != (tool + 1) %}
        RESPOND MSG="❌ ERRO: T{tool} não está ativa!"
        RESPOND MSG="💡 Execute T{tool} primeiro"
        {% set dummy = "return" %}
    {% endif %}
    
    # Posicionar sobre o sensor piezoelétrico
    RESPOND MSG="📍 Posicionando sobre sensor piezoelétrico..."
    G90  # Modo absoluto
    G1 X150 Y150 F6000  # Posição do sensor (ajustar conforme instalação)
    G1 Z10 F1200         # Altura segura
    
    # Realizar medição usando probe2 diretamente
    RESPOND MSG="🔍 Medindo com sensor piezoelétrico (probe2)..."
    PROBE PROBE=probe2
    
    {% set trigger_z = printer.probe.last_z_result|float %}
    RESPOND MSG="✅ Posição detectada: Z = {trigger_z|round(4)}mm"
    
    # Salvar resultado no estado do piezo
    SET_GCODE_VARIABLE MACRO=PIEZO_STATE VARIABLE=trigger_position VALUE={trigger_z}
    SET_GCODE_VARIABLE MACRO=PIEZO_STATE VARIABLE=triggered VALUE=True
    
    RESPOND MSG="📊 Calibração T{tool} concluída"
    RESPOND MSG="💾 Use SALVAR_OFFSET_PIEZO TOOL={tool} para salvar"

[gcode_macro SALVAR_OFFSET_PIEZO]
description: Salva offset calibrado com sensor piezoelétrico
gcode:
    {% set tool = params.TOOL|default(0)|int %}
    {% set ref_z = params.REFERENCE_Z|default(0.0)|float %}
    {% set trigger_z = printer["gcode_macro PIEZO_STATE"].trigger_position|float %}
    
    # Calcular offset relativo
    {% set offset_z = trigger_z - ref_z %}
    
    RESPOND MSG="💾 Salvando offset T{tool}..."
    RESPOND MSG="   Referência: {ref_z|round(4)}mm"
    RESPOND MSG="   Trigger: {trigger_z|round(4)}mm"
    RESPOND MSG="   Offset: {offset_z|round(4)}mm"
    
    # Salvar no variables.cfg conforme sistema existente
    {% if tool == 1 %}
        SAVE_VARIABLE VARIABLE=tool_0_offset_z VALUE={offset_z}
    {% elif tool == 2 %}
        SAVE_VARIABLE VARIABLE=tool_1_offset_z VALUE={offset_z}
    {% elif tool == 3 %}
        SAVE_VARIABLE VARIABLE=tool_2_offset_z VALUE={offset_z}
    {% endif %}
    
    RESPOND MSG="✅ Offset T{tool} salvo: {offset_z|round(4)}mm"

#####################################################################
#                    CALIBRAÇÃO AUTOMÁTICA DE TODAS AS FERRAMENTAS #
#####################################################################

[gcode_macro CALIB_OFF_AUTO]
description: Calibra offsets de todas as ferramentas usando sensor piezoelétrico
gcode:
    {% set tools_list = params.TOOLS.split(',') %}
    {% set ref_tool = params.REFERENCE_TOOL|default(0)|int %}
    
    RESPOND MSG="🔧 ===== CALIBRAÇÃO AUTOMÁTICA DE OFFSETS ====="
    RESPOND MSG="📌 Referência: T{ref_tool} (BLTouch)"
    RESPOND MSG="🔍 Ferramentas: {params.TOOLS}"
    RESPOND MSG="⚙️ Sensor: Piezoelétrico"
    
    # Estabelecer referência com T0 (BLTouch)
    RESPOND MSG="📍 Estabelecendo referência T{ref_tool}..."
    T{ref_tool}  # Ativar ferramenta de referência
    G28 Z        # Home Z com BLTouch
    
    # Mover para posição do sensor piezoelétrico
    G90
    G1 X150 Y150 F6000  # Posição do sensor
    
    # Medir referência com BLTouch
    PROBE
    {% set ref_z = printer.probe.last_z_result %}
    RESPOND MSG="✅ Referência T{ref_tool}: Z = {ref_z|round(4)}mm"
    
    # Calibrar cada ferramenta
    {% for tool_str in tools_list %}
        {% set tool = tool_str|int %}
        RESPOND MSG="🔧 Calibrando T{tool}..."
        
        # Ativar ferramenta
        T{tool}
        
        # Calibrar com piezo
        PIEZO_CALIBRATE TOOL={tool}
        
        # Salvar offset
        SALVAR_OFFSET_PIEZO TOOL={tool} REFERENCE_Z={ref_z}
        
        RESPOND MSG="✅ T{tool} calibrada"
    {% endfor %}
    
    # Aplicar offsets
    INIT_TOOL_OFFSETS
    
    RESPOND MSG="🎉 Calibração automática concluída!"
    RESPOND MSG="💾 Execute SAVE_CONFIG para salvar permanentemente"

#####################################################################
#                    MACROS DE TESTE E VERIFICAÇÃO                 #
#####################################################################

[gcode_macro TESTAR_SISTEMA_DUAL]
description: Testa funcionamento do sistema dual BLTouch + Piezo
gcode:
    RESPOND MSG="🧪 ===== TESTE DO SISTEMA DUAL ====="
    
    # Testar BLTouch
    RESPOND MSG="🔍 Testando BLTouch..."
    QUERY_PROBE
    
    # Testar sensor piezoelétrico
    RESPOND MSG="🔍 Testando sensor piezoelétrico..."
    QUERY_PIEZO
    
    # Testar precisão do BLTouch
    RESPOND MSG="📊 Testando precisão BLTouch..."
    PROBE_ACCURACY
    
    # Testar precisão do piezo
    RESPOND MSG="📊 Testando precisão piezoelétrico..."
    piezo_accuracy SAMPLES=5
    
    RESPOND MSG="✅ Teste do sistema dual concluído"

[gcode_macro STATUS_DUAL_PROBE]
description: Mostra status do sistema dual probe
gcode:
    RESPOND MSG="📊 ===== STATUS SISTEMA DUAL PROBE ====="
    
    # Status BLTouch
    {% set bltouch_offset = printer.configfile.settings.bltouch.z_offset %}
    RESPOND MSG="🔍 BLTouch (T0): Z offset = {bltouch_offset|round(4)}mm"
    
    # Status sensor piezoelétrico
    {% set piezo_triggered = printer["gcode_macro PIEZO_STATE"].triggered %}
    {% set piezo_pos = printer["gcode_macro PIEZO_STATE"].trigger_position %}
    RESPOND MSG="🔍 Sensor Piezo: {'ATIVADO' if piezo_triggered else 'LIVRE'}"
    RESPOND MSG="   Última posição: {piezo_pos|round(4)}mm"
    
    # Status offsets das ferramentas
    {% set vars = printer.save_variables.variables %}
    RESPOND MSG="🔧 Offsets das ferramentas:"
    RESPOND MSG="   T0: Z = 0.000mm (referência BLTouch)"
    
    {% if 'tool_0_offset_z' in vars %}
        RESPOND MSG="   T1: Z = {vars.tool_0_offset_z|round(4)}mm"
    {% else %}
        RESPOND MSG="   T1: Não calibrada"
    {% endif %}
    
    {% if 'tool_1_offset_z' in vars %}
        RESPOND MSG="   T2: Z = {vars.tool_1_offset_z|round(4)}mm"
    {% else %}
        RESPOND MSG="   T2: Não calibrada"
    {% endif %}
    
    {% if 'tool_2_offset_z' in vars %}
        RESPOND MSG="   T3: Z = {vars.tool_2_offset_z|round(4)}mm"
    {% else %}
        RESPOND MSG="   T3: Não calibrada"
    {% endif %}

#####################################################################
#                    DOCUMENTAÇÃO E INSTRUÇÕES                     #
#####################################################################

[gcode_macro HELP_DUAL_PROBE]
description: Mostra ajuda do sistema dual probe
gcode:
    RESPOND MSG="📚 ===== AJUDA SISTEMA DUAL PROBE ====="
    RESPOND MSG=""
    RESPOND MSG="🔍 SENSORES:"
    RESPOND MSG="   • BLTouch (T0): Homing, mesh, nivelamento"
    RESPOND MSG="   • Piezoelétrico: Calibração de offsets T1-T3"
    RESPOND MSG=""
    RESPOND MSG="🔧 COMANDOS PRINCIPAIS:"
    RESPOND MSG="   • QUERY_PIEZO - Verifica sensor piezoelétrico"
    RESPOND MSG="   • PIEZO_CALIBRATE TOOL=X - Calibra ferramenta X"
    RESPOND MSG="   • CALIBRAR_OFFSETS_AUTOMATICO - Calibra todas"
    RESPOND MSG="   • STATUS_DUAL_PROBE - Mostra status completo"
    RESPOND MSG=""
    RESPOND MSG="🧪 COMANDOS DE TESTE:"
    RESPOND MSG="   • TESTAR_SISTEMA_DUAL - Testa ambos sensores"
    RESPOND MSG="   • piezo_accuracy - Testa precisão piezo"
    RESPOND MSG=""
    RESPOND MSG="💡 FLUXO RECOMENDADO:"
    RESPOND MSG="   1. TESTAR_SISTEMA_DUAL"
    RESPOND MSG="   2. CALIBRAR_OFFSETS_AUTOMATICO"
    RESPOND MSG="   3. STATUS_DUAL_PROBE"
    RESPOND MSG="   4. SAVE_CONFIG"